apiVersion: app.s3folder.com/v1alpha1
kind: FolderService
metadata:
  name: example-folderservice2
  namespace: default
spec:
  userName: username2
  userSecret:
    name: username-secret
  platformSecrets:
    aws:
      credentials:
        name: iam-secret
  categories:
    - all
status:
  setupComplete: false
